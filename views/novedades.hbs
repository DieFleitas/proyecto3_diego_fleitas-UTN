<h2>Novedades</h2>
<div class="buscador">
    <form action="" method="get">
        <input type="search" name="q"/>
        <button type="submit">Buscar</button>
    </form>

</div>
{{#each novedades}}
        <div class="novedades">
            <h3>{{titulo}}</h3>
            <h4>{{subtitulo}}</h4>
            <p>{{{cuerpo}}}</p>
            <br>
        </div>
{{/each}}